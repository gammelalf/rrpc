[package]
name = "client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rrpc = { path = "../.." }
api = { path = "../api" }

tonic = { version = "~0.14", features = ["transport", "channel"] }
anyhow = { version = "~1" }
tokio = { version = "~1", features = ["full"] }